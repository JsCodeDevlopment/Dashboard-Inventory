import { DataTableReports } from "@/components/tables/reports";
import { NextPage } from "next";

const ReportsPage: NextPage = () => {
  return (
    <div>
      <DataTableReports />
    </div>
  );
};

export default ReportsPage;
